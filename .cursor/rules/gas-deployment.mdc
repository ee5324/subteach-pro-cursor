---
description: GAS 網址與部署原則：不代為 deploy，不覆蓋原檔
alwaysApply: true
---

# GAS Web App 原則

## 目前使用的部署（勿擅自更動）

- **部署作業 ID**：`AKfycbwonpXtF-j2C047CsYL060yD4bKzfw_caDDZw34aaZ6c13dPXknhLAi0nuswY_DRorG`
- **網頁應用程式 URL**：`https://script.google.com/macros/s/AKfycbwonpXtF-j2C047CsYL060yD4bKzfw_caDDZw34aaZ6c13dPXknhLAi0nuswY_DRorG/exec`

專案內 `config.ts` 的 `GAS_WEB_APP_URL` 與 Vercel 環境變數 `GAS_WEB_APP_URL` 必須與此 URL 一致。

## 禁止代為 deploy GAS（不覆蓋原檔）

- **不要**幫使用者執行 GAS 部署：不執行 `clasp push`、不執行 `clasp deploy`；**不覆蓋**目前已上線的 GAS 原檔。
- 當使用者說「部署」「push」「執行」等時，只執行 **Git**（與若有的 **Firebase deploy**），**不**執行任何 GAS 的 push 或 deploy，以免覆蓋 GAS 原檔。
- 若使用者日後要更新 GAS 程式，請提醒其自行在 GAS 網頁或本機執行 clasp，不要由 agent 代為執行。

## 若使用者明確提供新網址並要求更新

僅更新專案內 URL／部署 ID（config、本規則、部署整理文件、Vercel 提醒），**不**執行 clasp push 或 clasp deploy。

## 禁止

- 不得代為執行 `clasp push` 或 `clasp deploy`。
- 不得將 `GAS_WEB_APP_URL` 改為非上列 URL，除非使用者明確提供新的部署網址並要求更新。
